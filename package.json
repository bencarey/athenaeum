{
  "name": "athenaeum",
  "version": "1.1.1",
  "description": "Athenaeum — a knowledge container",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "setup": "bash scripts/setup.sh",
    "build:mac": "electron-builder --mac --publish never",
    "release": "npm version patch && git push --follow-tags"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "jsdom": "^24.1.0",
    "mammoth": "^1.8.0",
    "marked": "^12.0.2",
    "nanoid": "^3.3.7",
    "sanitize-html": "^2.13.0"
  },
  "devDependencies": {
    "electron": "^35.0.0",
    "electron-builder": "^26.0.12"
  }
}
